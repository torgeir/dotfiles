#jenv
#eval "$(jenv init - --no-rehash)"
#(jenv rehash &) 2> /dev/null

## exports below this must happen after jenv or else all hell breaks loose becaus of its shims

#nvm
. "$HOME/.nvm/nvm.sh"
nvm use 12
